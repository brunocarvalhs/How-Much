<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Tema de splash screen -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/primary</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="windowSplashScreenAnimationDuration">200</item>
        <item name="postSplashScreenTheme">@style/Theme.HowMuch</item>
    </style>

    <!-- Tema principal -->
    <style name="Theme.HowMuch" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Cores principais -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_container</item>
        <item name="colorAccent">@color/secondary_light</item>

        <!-- Fundo e superfícies -->
        <item name="android:colorBackground">@color/background_light</item>
        <item name="android:windowBackground">@color/background_light</item>

        <!-- Cores de texto -->
        <item name="android:textColorPrimary">@color/on_background_light</item>
        <item name="android:textColorSecondary">@color/on_surface_light</item>
    </style>
</resources>
